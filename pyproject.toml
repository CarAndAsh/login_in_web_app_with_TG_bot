[project]
name = "login-in-web-app-with-tg-bot"
version = "0.1.0"
description = "At start it's simple app to show user data from Telegram by using bot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[dependency-groups]
dev = [
    "uv>=0.9.18",
]

[tool.uv.workspace]
members = [
    "app",
    "bot",
]
